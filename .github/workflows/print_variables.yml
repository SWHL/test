name: Event Loggger
on:
  push:
    branches: main

jobs:
  log-github-event-goodies:
    name: "LOG Everything on GitHub Event"
    runs-on: ubuntu-latest
    steps:
      - name: Pull latest code
        uses: actions/checkout@v3

      - name: Logging
        env:
            ZIP_NAME: RapidVideOCR-${{ runner.arch }}-${{ runner.os }}-no-installer.zip
        run: |
          echo ${{ env.ZIP_NAME }}
