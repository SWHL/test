name: Release

on:
  push:
    tags:
    - Desktop-v*

jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest

    steps:
    - name: Print tag info
      run: |
        echo "VERSION=${{ github.ref_name }}"
